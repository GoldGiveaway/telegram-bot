syntax = "proto3";

package giveaway;

service Greeter {
    rpc SayGiveaway (GiveawayRequest) returns (GiveawayReply) {}
}

message GiveawayRequest {
    string token = 1;
    string initData = 2;
}

message GiveawayReply {
    string json_message = 1;
}